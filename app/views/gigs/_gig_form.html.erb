<%= simple_form_for [slot, gig] do |f| %>
  <%= f.input :name, error: "You must give it a name" %>
  <%= f.input :description, error: "Just write something..." %>
  <%= f.button :submit, class: 'btn btn-success' %>
<% end %>
